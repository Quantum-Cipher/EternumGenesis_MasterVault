# 📦 Vault archives and checksums (never commit)
vault/**/*.zip
vault/**/*.sha256

# 🔐 Private keys and sensitive credentials
*.pem
*.key
private.key
public.key

# 🗑️ System trash and old backups
.Trash/
*.old
*.backup
*.bak
